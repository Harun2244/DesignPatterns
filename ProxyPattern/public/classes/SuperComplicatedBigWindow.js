export class SuperComplicatedBigWindow {
    constructor(length, width) {
        this.length = length;
        this.width = width;
    }
    getSurfaceArea() {
        return this.length * this.width;
    }
}
